"use strict";
{
    function add(num1, num2) {
        return num1 + num2;
    }
    add(3, '');
    const arr = (num1, num2) => num1 + num2;
    const poorUser = {};
    const ar = [1, 2, 3, 4];
    const newr = ar.map((ele) => ele * ele);
}
